import { NavigationContainer } from "@react-navigation/native";
import { StackCustom } from "./stack"

export default function routes() {
    return (
        <NavigationContainer>

        </NavigationContainer>
    )
}